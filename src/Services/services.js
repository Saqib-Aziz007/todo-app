// import { AlipaySquareFilled } from "@ant-design/icons";

// const Services = async () => {
//   const _apiUrl = "https://jsonplaceholder.typicode.com/todos/";

//   const getTodo = () => {
//     const response = await fetch(_apiUrl);
//   };

//   const deleteTodo = async ({ id }) => {
//     const response = await fetch(_apiUrl + id);
//   };

//   const updateTodo = async (id, data) => {
//     const response = await fetch(_apiUrl + id, { method: "PUT", body: data });
//   };

//   const createTodo = async (data) => {
//     const response = await fetch(_apiUrl, { method: "POST" });
//   };
// };

// export default Services;
